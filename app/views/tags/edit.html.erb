<ul class="uk-breadcrumb">
  <li><%= link_to "List", admin_tags_path %></li>
  <li>Edit</li>
</ul>

<div class="uk-card uk-card-default uk-card-body">
  <%= form_with(model: @tag, url: admin_tag_path(@tag), local: true) do |form| %>
    <%= render partial: "form", locals: {form: form} %>
  <% end %>
</div>